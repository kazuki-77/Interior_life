<div>
  <%= attachment_image_tag @post_image, :image, :fill, 300, 300 %>
  <p><%= @post_image.title %></p>
  <p><%= @post_image.caption %></p>
  <p><%= @post_image.user.name %></p>
  <p><%= @post_image.created_at.strftime('%Y/%m/%d') %>
    <% if @post_image.user == current_user %>
      <%= link_to '削除', post_image_path(@post_image), method: :delete %>
    <% end %>

    <% if @post_image.favorited_by?(current_user) %>
      <%= link_to @post_image.favorites.count, post_image_favorites_path(@post_image), method: :delete, class: "fas fa-heart heart-red" %>
    <% else %>
      <%= link_to @post_image.favorites.count, post_image_favorites_path(@post_image), method: :post, class: "fas fa-heart heart-blue" %>
    <% end %>
  <i class="fas fa-comment"></i> <%= @post_image.post_comments.count %>
  </p>

  <div>
    <% @post_image.post_comments.each do |post_comment| %>
      <p><%= attachment_image_tag post_comment.user, :profile_image, :fill, 60, 60, fallback: "no-image.jpg", class: "rounded-circle" %></p>
      <%= post_comment.user.name %>
      <%= post_comment.created_at.strftime('%Y/%m/%d') %>
      <div>
        <%= post_comment.comment %>
        <% if post_comment.user == current_user %>
            <%= link_to '削除', post_image_post_comment_path(post_comment.post_image, post_comment), method: :delete, class: "btn btn-danger" %>
        <% end %>
      </div>
    <% end %>
  </div>
  <div>
    <%= form_with model: [@post_image,@post_comment], local: true do |f| %>
      <%= f.text_area :comment, rows:'5', placeholder: "コメント" %>
      <%= f.submit "送信", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>