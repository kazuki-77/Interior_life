<div class="container">
  <div class="row">
    <%= attachment_image_tag @user, :profile_image, :fill, 100, 100, fallback: "no-image.jpg", class: "rounded-circle" %>
    <% if @user == current_user %>
      <%= link_to "編集", edit_user_path(@user), class: "btn btn-secondary text-right" %>
    <% end %>
    <!--< if @user != current_user %>-->
    <!--  < if @user.is_followed_by?(current_user) %>-->
    <!--    <= link_to "フォロー外す", user_relationships_path(@user), method: :delete %>-->
    <!--  < else %>-->
    <!--    <= link_to "フォローする", user_relationships_path(@user), method: :post %>-->
    <!--  < end %>-->
    <!--< end %>-->
  </div>
  <hr>
  <div class="row">
    <p><%= @user.name %></p>
  </div>
  <div class="row">
    <p><%= @user.introduction %></p>
  </div>
  <div class="row">
    <%#= link_to "#{@user.followings.count} フォロー中", following_user_path(@user) %>
    <%#= link_to "#{@user.followers.count} フォロワー", followers_user_path(@user) %>
  </div>
  <div class="row">
    <%= render 'post_images/index', post_images: @post_images %>
  </div>
</div>
